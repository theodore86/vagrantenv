#!/usr/bin/env bash
set -euo pipefail

if ! command -v 'bundle' >/dev/null; then
  echo "Executable \`bundle\` not found"
  exit 1
fi

bundle install -j "$(nproc)" &>/dev/null
bundle exec rubocop "$@"
