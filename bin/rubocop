#!/usr/bin/env bash
set -euo pipefail

[[ $(command -v 'bundle' >/dev/null) ]] || \
    { echo "Executable \`bundle\` not found"; exit 1; }

bundle install -j "$(nproc)" &>/dev/null
bundle exec rubocop "$@"
